{
  "name": "starfleet-command-simulator",
  "version": "1.0.0",
  "description": "An offline Starfleet command simulator. Real orders, real destinations, real consequences.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node tools/serve.mjs",
    "test": "node --test tests/*.test.js",
    "icons": "node tools/gen-icons.mjs",
    "build": "node tools/build-single.mjs",
    "verify": "node --test tests/*.test.js && node tools/verify-app.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
